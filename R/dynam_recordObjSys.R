#dyam_recordObjSys
#ObjSys_eg = list(
#  global = list(const = list(alpha = 1e-7)),#Gravitational
#  BW = list(const = c(R =.05,K = 150000),
#           var = c(p = 5000)),
#  FW = list(const = c(R =.08,K = 400000),
#           var = c(p = 7000))
#)

#dynam_update_ObjSys = function(x,f,record_per,record_type = ){

#}
